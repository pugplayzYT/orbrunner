# OrbRunner build properties
# Edit orbrunnerServerUrl to change where the launcher connects by default.
# Rebuild the launcher JAR after changing this: ./gradlew :launcher:jar
orbrunnerServerUrl=http://localhost:5000
