# v3.4 - Fixed PR Auto-Close Workflow

## Release Date: February 27, 2026

Fixed the auto-close external PRs GitHub Actions workflow to run without syntax errors.

### What changed

- **CI Workflow Fix** — Updated formatting in the auto-close-prs action to prevent YAML syntax parsing errors that broke the CI run
